# Core web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
websockets==14.1

# Database
SQLAlchemy==2.0.36
# PostgreSQL driver (used in production / docker-compose; SQLite needs nothing).
psycopg[binary]==3.2.3

# Settings / validation
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# Security / auth
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
python-jose[cryptography]==3.3.0

# Numeric (audio buffer handling)
numpy==2.2.1
