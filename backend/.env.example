# Copy to `.env` and adjust for your environment.

# --- General ---
APP_NAME=Smart Meeting
ENVIRONMENT=development
DEBUG=true

# --- Security (CHANGE IN PRODUCTION) ---
JWT_SECRET_KEY=change-me-to-a-long-random-string
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080
BCRYPT_ROUNDS=12

# --- Database ---
# SQLite (default):
DATABASE_URL=sqlite:///./smart_meeting.db
# PostgreSQL example:
# DATABASE_URL=postgresql+psycopg://user:pass@localhost:5432/smartmeeting

# --- CORS (comma-separated) ---
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# --- Storage ---
AUDIO_STORAGE_DIR=./data/audio

# --- Audio ---
AUDIO_SAMPLE_RATE=16000
AUDIO_CHANNELS=1

# --- Whisper (two-pass) ---
# Prefer int8 on CPU to avoid OOM when loading the finalization model.
# For GPU / full accuracy: WHISPER_FINAL_MODEL=large-v3 WHISPER_DEVICE=cuda WHISPER_COMPUTE_TYPE=float16
WHISPER_LIVE_MODEL=base
WHISPER_FINAL_MODEL=medium
WHISPER_DEVICE=cpu
WHISPER_COMPUTE_TYPE=int8
# "hil" (Hiligaynon) is not a Whisper token — the app auto-detects language
# instead of forcing Tagalog, which caused English hallucination loops.
WHISPER_DEFAULT_LANGUAGE=hil

# --- LLM (BART / mBART) ---
BART_MODEL=facebook/bart-large-cnn
MBART_MODEL=facebook/mbart-large-50-many-to-many-mmt
ALLOW_LLM_FALLBACK=true

# --- Rate limiting ---
RATE_LIMIT_AUTH=20/minute
RATE_LIMIT_AI=30/minute
